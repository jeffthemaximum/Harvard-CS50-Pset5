#include <stdio.h>
#include <string.h>

 

/*

    for(i=0; i<MAX; i++)
    {
        a[i] = i;
        b[i] =a[i];
        printf("a[%d] = %d ... b[%d] = %d\n", i, a[i], i, b[i]);
    }
        return 0;
        
*/
